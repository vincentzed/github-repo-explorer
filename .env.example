# Optional: GitHub Personal Access Token for higher rate limits (30 req/min instead of 10)
# Create at: https://github.com/settings/tokens with public_repo or no scopes for public repos
GITHUB_TOKEN=ghp_your_token_here

# Server port (optional, defaults to 3001)
PORT=3001